services:
  - type: web
    name: mariiskii-narod
    runtime: static
    buildCommand: echo "Static site — nothing to build"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html